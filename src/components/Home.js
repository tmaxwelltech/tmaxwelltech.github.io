import React from 'react';
import { Jumbotron, Container} from 'reactstrap';
import Cards from './Cards'

const Home = (props) => {
  return (
    <div>
 
      <Cards/>
    </div>
  );
};

export default Home;